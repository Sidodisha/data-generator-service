spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myextractordb?useSSL=false&serverTimezone=UTC
    username: root
    password: newmysql@25
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # options: validate | update | create | create-drop
    show-sql: true